<!-- res/drawable/button_background.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 按钮被按下时的背景 -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#D3D3D3" /> <!-- 按下时的颜色，这里使用灰色 -->
            <!-- 描边只在顶部和右侧 -->
            <stroke android:width="1dp" android:color="#eeeeee" />
        </shape>
    </item>

    <!-- 按钮默认状态下的背景 -->
    <item>
        <layer-list>
            <item>
                <shape android:shape="rectangle">
                    <stroke android:width="1dp" android:color="#eeeeee" /> <!-- 边框颜色 -->
                    <solid android:color="#FFFFFF" /> <!-- 按钮背景颜色 -->
                </shape>
            </item>
            <item android:left="-1dp" android:bottom="1dp" android:right="1dp" android:top="1dp">
                <shape android:shape="rectangle">
                    <solid android:color="#FFFFFF" />
                </shape>
            </item>
        </layer-list>
    </item>
</selector>
